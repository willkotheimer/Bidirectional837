<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Translator.Domain\Translator.Domain.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- PROVENANCE: ADR-013 - the seed corpus is embedded rather than copied beside the binary, so a
       deployed instance carries its reference data with it and cannot start with an empty catalog. -->
  <ItemGroup>
    <EmbeddedResource Include="..\..\seed\hcpcs_categories.csv" LogicalName="Translator.Generation.Seed.hcpcs_categories.csv" />
    <EmbeddedResource Include="..\..\seed\charges_sample.csv" LogicalName="Translator.Generation.Seed.charges_sample.csv" />
    <EmbeddedResource Include="..\..\seed\providers_by_state.csv" LogicalName="Translator.Generation.Seed.providers_by_state.csv" />
  </ItemGroup>

</Project>
